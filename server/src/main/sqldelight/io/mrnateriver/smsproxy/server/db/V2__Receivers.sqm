CREATE TABLE receivers (
    receiverKeyHash BYTEA NOT NULL PRIMARY KEY,
    publicKey BYTEA NOT NULL,
    fcmKey VARCHAR(255) NOT NULL,
    createdAt TIMESTAMPTZ NOT NULL,
    updatedAt TIMESTAMPTZ NULL
);
